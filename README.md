
  # Personal site with horizontal scroll

  This is a code bundle for Personal site with horizontal scroll. The original project is available at https://www.figma.com/design/cTpKz9SNvOmCSMsnQrzz5j/Personal-site-with-horizontal-scroll.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  